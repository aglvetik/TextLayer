<Window x:Class="TextLayer.App.Views.RegionSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:TextLayer.App.Services"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowActivated="True"
        Focusable="True">
    <Grid Background="#38000000"
          Cursor="Cross">
        <Canvas x:Name="SelectionCanvas"
                Background="Transparent">
            <Border x:Name="SelectionBorder"
                    Visibility="Collapsed"
                    Background="#243A72FF"
                    BorderBrush="#FF5B8DEF"
                    BorderThickness="1"
                    CornerRadius="4" />
        </Canvas>

        <Border HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="18"
                Padding="12,8"
                Background="#CC0D1524"
                CornerRadius="999">
            <TextBlock Text="{Binding Source={x:Static services:UiTextService.Instance}, Path=[RegionSelection.Hint]}"
                       Foreground="White"
                       FontSize="13"
                       FontWeight="SemiBold" />
        </Border>
    </Grid>
</Window>
